# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment / secrets
.env
env

# Coverage
htmlcov/
.coverage
coverage.xml

# IDE
.idea/
.vscode/
*.swp
*.swo

# Git worktrees
.worktrees/

# Runtime (BUILD_ID stamp target — DEPLOY_TARGET=systemd writes /run/usa-wa/build-id)
/run/

# Brainstorming companion session artifacts
.superpowers/

# Per-worktree port allocation (using-git-worktrees); never repo content
.port

# dbt pipeline (#303): local duckdb + dbt run artifacts; never repo content
data/
packages/usa-wa-pipeline/dbt/target/
packages/usa-wa-pipeline/dbt/logs/
packages/usa-wa-pipeline/dbt/.user.yml
.coverage.*
# Raw-tier file store (#304) when rooted in the checkout (USA_WA_RAW_ROOT default)
raw/
