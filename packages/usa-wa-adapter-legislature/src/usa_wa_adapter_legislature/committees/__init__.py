"""The ``committees-roster:<biennium>`` archive — WSL ``GetCommittees`` committee identity.

Phase A ``harvest`` archives the wire; ``cohort`` re-parses it offline; ``lifecycle``,
``seed`` and the ``lineage_*`` / ``succession_*`` modules derive committee existence and
operator-attested lineage from it. Membership rosters are a **separate** archive — see
:mod:`usa_wa_adapter_legislature.membership`.
"""
