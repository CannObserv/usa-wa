"""The ``usa_wa_operator`` attestation source — operator-stated succession facts.

Not a wire. An operator states a fact (``departed`` / ``vacated`` / ``seated``) that no WSL
endpoint dates, ``store`` persists it with provenance, the span builders overlay it, and
``invariants`` is the backstop for the event nobody entered.
"""
