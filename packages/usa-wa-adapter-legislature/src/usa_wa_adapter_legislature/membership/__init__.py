"""The ``committee-members-hist:`` archive — WSL ``GetCommitteeMembers`` rosters.

Phase A ``harvest`` archives the wire; ``cohort`` re-parses it offline; ``projector`` ->
``build`` -> ``emit`` turn it into committee-membership tenure Assignments.
"""
