"""Conformed-tier row builders (#309): registry ⨝ staging, stateless.

Pure logic here so pytest owns red/green; the dbt Python models under
``dbt/models/conformed/`` are thin adapters (docs/PIPELINE.md § TDD).
"""
