# Judged losses, newest last. WARRANT :: CONTENT
#
# The #202 (Batch E) entries were pruned in the #263 round: each acknowledged a
# line that no longer exists at any base, so every run re-reported them as
# "matched nothing" — noise in the one report that must not be skimmed.
#
# Each line below was read against its replacement before being warranted.

# The MODULES-SYNC.md index entry described a doc that this same run split; the
# producer CLIs it names now live in docs/MODULES-SYNC-PRODUCERS.md, which gets
# its own entry on the next line of the list.

# The count is part of the pointer: this run added a tenth docs/MODULES-*.md, so
# "the nine" points at a list that no longer exists. Replaced by "the ten".

# This lead-in introduced a fenced block whose two commands are verbatim in
# § Common Commands of the same file (the export line three times over, the
# uvicorn line twice). The block was dropped under the duplication warrant; the
# rule it carried — port 8001, load env first — survives in the rewritten
# sentence, which now points at § Common Commands instead of a colon.

# --- #263 follow-up: the worktree-venv opt-out ---

# The three-line comment restated the advisory-lock behaviour verbatim held in
# docs/COMMANDS.md (the lock, the TEST_DATABASE_LOCK_TIMEOUT default, the error
# string) and again in docs/ENVIRONMENT.md's entry for that variable. Compressed
# to the rule plus a pointer; nothing about the behaviour is now unstated.
duplicate :: # Concurrent sessions (other worktrees) serialize on a Postgres advisory lock (#208):
duplicate :: # a second db-marked run queues up to TEST_DATABASE_LOCK_TIMEOUT s (default 600), then
duplicate :: # fails loudly — "another pytest session holds the test database"

# Both pointers said docs/CODE-EXPLORATION.md CARRIES the prefetch query. This
# same change removed the query from that doc — it had drifted, omitting three
# graph tools — and pointed it at socraticode-reminder.sh, the hook that emits
# the query verbatim every session. So the pointer's target moved in this run
# and both lines now say "the source of" instead.
retarget :: finding, and where the session-start `ToolSearch` prefetch query comes from:
retarget :: - [docs/CODE-EXPLORATION.md](docs/CODE-EXPLORATION.md) — goal→tool table, the broken file-dependency graph, where the `ToolSearch` prefetch query comes from

# --- #263 CR round 2 -------------------------------------------------------
# The #263 round demoted this line into docs/ARCHITECTURE.md verbatim. Review
# found the copy did two wrong things at once: its leading pointer told the
# reader to read the document they were already in, and the rest restated what
# that document already says at length. Read clause by clause against the
# destination before deleting it — every one is already there:
#
#   one package per jurisdiction+target, sources inside  -> ## One package per *target*
#   source = transport/adapter/normalize/cohort/harvest  -> the tree in that section
#   application source-agnostic via a cohort interface   -> same section, opening
#   usa-wa-adapter-sos as the worked example             -> ## Worked example — WA SOS House Position
#   audit coverage before building on a source           -> ## Audit before you build
#   never key a parser on an exact upstream string       -> ARCHITECTURE.md:222, and kept
#                                                           inline in AGENTS.md besides
#   #183 flat top level / subpackage on a varying axis   -> ## When a target publishes only one source (#183)
#   harvest.py = Phase A, build.py = Phase B             -> same section, and kept inline
#
# So the demotion's destination already held the content; what moved was a
# summary of it. AGENTS.md keeps the pointer plus the two always-on rules.
duplicate :: **Read [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) before adding an adapter, a data source, or a span/seat builder.** It is the reusable Layer-3 pattern: one adapter package per *jurisdiction+target* bundling every source that target publishes; each **source** a self-contained archive (own `Source`/`source_slug`/archive-key/transport/adapter/normalize/cohort/harvest); the **application** (spans/seats) source-agnostic, consuming a cohort interface — so a fact can draw on a new source without a rewrite (the `usa-wa-adapter-sos` filings + results sources are the worked example). Audit a source's coverage before building on it; never key a parser on an exact upstream string. Inside a package (#183): single-source target ⇒ flat top level (no `pdc/` inside `usa_wa_adapter_pdc`); subpackage only on an axis that varies (WSL splits on its four archives); **`harvest.py` = Phase A, `build.py` = Phase B**, plus `projector`/`emit`/`migrate_*`.


# #399 round: § Archive vs rebuild moved out of docs/COMMANDS.md (over its
# 10,000-token budget) into docs/COMMANDS-SEATS.md, beside the seat-fact builders
# whose daily re-drives it governs; heading kept verbatim, so its (#201) anchor
# slug survives under the new file. The in-block pointer in § Data refresh named
# "§ Archive vs rebuild, below", which no longer exists below; it now names
# COMMANDS-SEATS.md.
docs/COMMANDS.md :: retarget :: # B) — one unit each. Flag semantics across the halves: § Archive vs rebuild, below.
# The two inbound links to that section, retargeted at its new home.
docs/COMMANDS-BACKFILL.md :: retarget :: (#201) live beside the daily commands in
docs/COMMANDS-BACKFILL.md :: retarget :: [COMMANDS.md](COMMANDS.md#archive-vs-rebuild--which-half-each-flag-governs-201).
docs/MODULES-FACTS-SEATS.md :: retarget :: [COMMANDS.md](COMMANDS.md#archive-vs-rebuild--which-half-each-flag-governs-201). **Two new unit files —

# AGENTS.md round, 2026-09-22 (6,072 -> 5,924 exact). Each line read against its
# replacement. --claims: 146 atoms at base, 0 dropped — every tightening below
# kept its backticked spans, issue refs and links.
#
# disproven — three claims a command refuted:
#   .socraticodecontextartifacts.json is a hand-kept manifest (one key,
#   "artifacts", 29 entries), not where codebase_index writes the index;
#   the workspace is six layers (the table two lines down, import-linter's
#   contracts), four was the MVP spec's; the unit tier ran 91 s without
#   coverage and 196-208 s with it today, not ~11 s / ~27 s; and CODE-EXPLORATION.md says the
#   graph works (1,633 edges) — it is not "broken".
AGENTS.md :: disproven :: (once indexed; the index lives in `.socraticodecontextartifacts.json` once `codebase_index` has
AGENTS.md :: disproven :: `uv` workspace. Four-layer clearinghouse split
AGENTS.md :: disproven :: # Add --no-cov for a faster (~11s vs ~27s), ungated inner loop
AGENTS.md :: disproven :: — goal→tool table, the broken file-dependency graph, the `ToolSearch` prefetch
# tighten — same claims, fewer words. The graph paragraph's edge and file counts
# are CODE-EXPLORATION.md's measurement table; the uvicorn paragraph's mechanism
# is LOGGING.md's first section; the ARCHITECTURE.md read-before clause is
# § Project Layout's bold rule; the two DEPLOYMENT.md sentences keep every item.
AGENTS.md :: tighten :: **The file-dependency graph works here since SocratiCode 1.13.0 (#299)** — 1,633 edges across
AGENTS.md :: tighten :: 510 files, `grep`-verified; `codebase_graph_query` and the file-mode of `codebase_impact` are
AGENTS.md :: tighten :: answers now, not traps. On an older engine it silently resolves almost nothing, so check the
AGENTS.md :: tighten :: builder version `codebase_graph_status` reports before trusting an empty answer. Goal→tool table,
AGENTS.md :: tighten :: the measurement, and the session-start `ToolSearch` prefetch:
AGENTS.md :: tighten :: The full service table (every systemd unit and what each one does)
AGENTS.md :: tighten :: The per-unit restart table, the `uv sync --locked` rationale, and the `verify-units.sh` pre-commit gate (#51) are in
AGENTS.md :: tighten :: **Under uvicorn, `configure_logging()` alone is not enough** (#155)
AGENTS.md :: tighten :: — the reusable Layer-3 pattern; read before adding an adapter, a source, or a span/seat builder
# Same stale fact in docs/SKILLS.md, found by this round's seam sweep: the
# health hook run with SOCRATICODE_HEALTH_FORCE=1 on 2026-09-22 printed nothing
# (silent = nothing to report), so the edge-yield gate does not fire daily.
docs/SKILLS.md :: disproven :: That last one fires here every day and is expected

# #237 branch, curating-context round (2026-09-24): six over-budget docs split. Each line
# below is a pointer whose target this same change moved; read against its replacement.
AGENTS.md :: retarget :: Recovery and the start-limit reasoning: [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md).
docs/DEPLOYMENT.md :: retarget :: runbook: [COMMANDS-BACKFILL.md](COMMANDS-BACKFILL.md) § Roster PDF
docs/COMMANDS.md :: retarget :: happens in a worktree (`docs/DEPLOYMENT.md` § Main-only checkout), and a new one
docs/MODULES-LEGISLATURE-ROSTER.md :: retarget :: — the sequence is in COMMANDS-BACKFILL.md
docs/PIPELINE-CONFORMED.md :: retarget :: engine as a Python model) landed next — see below; roles/seats complete the
docs/SOCRATICODE.md :: retarget :: [`docs/DEPLOYMENT.md` § Memory pressure](DEPLOYMENT.md#memory-pressure-issue-389).
docs/SKILLS.md :: retarget :: ([`docs/DEPLOYMENT.md` § Shared-venv integrity](DEPLOYMENT.md#shared-venv-integrity-issue-279))
docs/PIPELINE.md :: retarget :: carries: [`PIPELINE-CONFORMED.md`](PIPELINE-CONFORMED.md).
