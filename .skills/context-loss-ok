# Judged losses, newest last. WARRANT :: CONTENT
#
# The #202 (Batch E) entries were pruned in the #263 round: each acknowledged a
# line that no longer exists at any base, so every run re-reported them as
# "matched nothing" — noise in the one report that must not be skimmed.
#
# Each line below was read against its replacement before being warranted.

# The MODULES-SYNC.md index entry described a doc that this same run split; the
# producer CLIs it names now live in docs/MODULES-SYNC-PRODUCERS.md, which gets
# its own entry on the next line of the list.

# The count is part of the pointer: this run added a tenth docs/MODULES-*.md, so
# "the nine" points at a list that no longer exists. Replaced by "the ten".

# This lead-in introduced a fenced block whose two commands are verbatim in
# § Common Commands of the same file (the export line three times over, the
# uvicorn line twice). The block was dropped under the duplication warrant; the
# rule it carried — port 8001, load env first — survives in the rewritten
# sentence, which now points at § Common Commands instead of a colon.

# --- #263 follow-up: the worktree-venv opt-out ---

# The three-line comment restated the advisory-lock behaviour verbatim held in
# docs/COMMANDS.md (the lock, the TEST_DATABASE_LOCK_TIMEOUT default, the error
# string) and again in docs/ENVIRONMENT.md's entry for that variable. Compressed
# to the rule plus a pointer; nothing about the behaviour is now unstated.
duplicate :: # Concurrent sessions (other worktrees) serialize on a Postgres advisory lock (#208):
duplicate :: # a second db-marked run queues up to TEST_DATABASE_LOCK_TIMEOUT s (default 600), then
duplicate :: # fails loudly — "another pytest session holds the test database"

# Both pointers said docs/CODE-EXPLORATION.md CARRIES the prefetch query. This
# same change removed the query from that doc — it had drifted, omitting three
# graph tools — and pointed it at socraticode-reminder.sh, the hook that emits
# the query verbatim every session. So the pointer's target moved in this run
# and both lines now say "the source of" instead.
retarget :: finding, and where the session-start `ToolSearch` prefetch query comes from:
retarget :: - [docs/CODE-EXPLORATION.md](docs/CODE-EXPLORATION.md) — goal→tool table, the broken file-dependency graph, where the `ToolSearch` prefetch query comes from
