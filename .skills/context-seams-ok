# Judged seams from the #202 curation (Batch E). One entry per line.
#
# Two legitimate classes, both pre-existing repo conventions that this run did
# not create and must not silently "fix":
#
# 1. back-reference — a reference doc pointing back at the AGENTS.md section it
#    was demoted from. Every anchor below was re-verified against the edited
#    AGENTS.md: § Code Exploration Policy, § Common Commands, § Server Lifecycle
#    and § Environment Variables all still exist, and measure-context reports
#    dead_anchors: []. The pointer is the navigational contract, not a stale ref.
#
# 2. provenance-heading — an issue number inside a heading. The repo uses this
#    deliberately across nine docs; the numbers are the design record, and the
#    slugs they generate are already load-bearing anchors (COMMANDS-BACKFILL.md
#    links COMMANDS.md#archive-vs-rebuild--which-half-each-flag-governs-201,
#    COMMANDS-SEATS.md#… since the #399 round moved the section there).
#    Renaming them here would break links this same run wired.

# --- back-references to AGENTS.md ------------------------------------------
docs/ARCHITECTURE.md :: and the module in [`AGENTS.md`](../AGENTS.md).
docs/COMMANDS.md :: The everyday subset is in [`AGENTS.md`](../AGENTS.md#common-commands).
docs/COMMANDS.md :: daily/weekly/monthly ones on systemd timers ([`AGENTS.md`](../AGENTS.md#server-lifecycle) § Server
docs/COMMANDS.md :: restart matrix, and [`AGENTS.md`](../AGENTS.md#server-lifecycle) § Server Lifecycle
docs/DEPLOYMENT.md :: that apply on nearly every task stay in [`AGENTS.md`](../AGENTS.md); this is the
docs/ENVIRONMENT.md :: [`AGENTS.md`](../AGENTS.md#environment-variables); the DB roles these DSNs map
docs/SKILLS.md :: | `curating-context` | Curate the agent-context surface (`AGENTS.md` + the docs it links)
docs/SKILLS.md :: `curating-context` (#161) keeps `AGENTS.md` and the docs it links under a token budget

# --- provenance in heading text --------------------------------------------
docs/ARCHITECTURE.md :: When a target publishes only one source (#183)
docs/COMMANDS-BACKFILL.md :: Historical backfill (epic #76 / sub-project 3 / #100)
docs/COMMANDS-SEATS.md :: PDC historical backfill (#79)
docs/COMMANDS-SEATS.md :: WSL+SOS House Position backfill (#101)
docs/COMMANDS-SUCCESSION.md :: Senate odd-year corroboration (#123)
docs/COMMANDS-SUCCESSION.md :: House odd-year special-winner corroboration (#149)
docs/COMMANDS-SUCCESSION.md :: Operator succession (#107)
docs/COMMANDS-SUCCESSION.md :: Committee lineage & lifecycle (#124)
docs/COMMANDS-SEATS.md :: Archive vs rebuild — which half each flag governs (#201)
docs/DEPLOYMENT.md :: Failure alerting (#49)
docs/DEPLOYMENT.md :: DB role topology (defense-in-depth, issue #22)
docs/DEPLOYMENT-HOST.md :: Main-only checkout (issue #87)
docs/DEPLOYMENT.md :: Deploy convention: units never sync the venv (issue #30)
docs/DEPLOYMENT.md :: Validating unit edits (#51)
docs/LWW-NOOP-GATE.md :: Sibling: the rejected-UPDATE replay guard (#132)
docs/MODULES-FACTS-SEATS.md :: The archive/rebuild split (#201)
docs/MODULES-LEGISLATURE.md :: Package layout (#183)

# --- #263 round: one seam this run created, two verified truthful ----------
# (a second, in docs/ARCHITECTURE.md, was removed in CR round 2 along with the
#  near-duplicate section that carried it — so its ack was pruned with it.)
# Read against the edited AGENTS.md, which still carries § Conventions and
# § Infrastructure; measure-context reports dead_anchors: [].
docs/LOGGING.md :: The rules an agent needs on most tasks stay in [`AGENTS.md`](../AGENTS.md) § Conventions:
docs/DEPLOYMENT.md :: `AGENTS.md` against its 6,000-token budget, plus the doc tree and cross-reference
docs/SKILLS.md :: [`AGENTS.md`](../AGENTS.md) § Infrastructure fixes this repo as a **single-VM setup

# --- provenance-headings never acked, same class as the block above --------
# Issue numbers in headings, in docs this run did not touch. Judged legitimate
# under class 2 above: the numbers are the design record and the slugs are
# load-bearing. Acked so the ledger's seam count reflects judgement, not backlog.
docs/COMMANDS-ROSTER.md :: ## Roster PDF — the archival member source (#225, epic #219)
docs/COMMANDS-ROSTER.md :: ## Pre-1991 build (#228) — roster Persons, party spans, Senate seat spans
docs/COMMANDS-SYNC.md :: ### Retract spurious anchored assignments (#144 Phase 2)
docs/COMMANDS-SYNC.md :: ### Retire superseded outbox rejections (#258)
docs/DEPLOYMENT.md :: ## Scheduled work outside systemd (#221)
docs/LWW-NOOP-GATE.md :: ## The inverse: the branch that never fires (#247)


# --- #399 round: tracked source naming docs/COMMANDS.md --------------------
# Swept because § Archive vs rebuild left the file. All three name COMMANDS.md
# for content that stayed: the manifest entry for the doc itself, the uvicorn
# --log-config line under § Dev server, and the test commands under § Tests.
.socraticodecontextartifacts.json :: "path": "./docs/COMMANDS.md",
packages/usa-wa-api/tests/test_log_config.py :: for doc in ("README.md", "AGENTS.md", "docs/COMMANDS.md"):
scripts/tests/test_coverage_profiles.py :: DOCS_WITH_TEST_COMMANDS = ("AGENTS.md", "README.md", "docs/COMMANDS.md")

# --- AGENTS.md round 2026-09-22: tracked source naming AGENTS.md ---------------
# Swept because content left the policy file. Judged one by one: each names a
# section that still exists under the same heading (§ Server Lifecycle x16,
# § Environment Variables x5, § Project Layout x3), a rule still stated there
# (never key a parser on an exact upstream string; codebase_search over grep;
# worktree-per-feature), or the file itself as a path/fixture. None pointed at
# a line this round rewrote.
.claude/hooks/context-budget-guard.sh :: # AGENTS.md, the next fortnight of edits puts it back, and the telemetry trend
.claude/hooks/context-budget-guard.sh :: - the policy file: AGENTS.md or CLAUDE.md at the repo root. A symlinked
.claude/hooks/context-budget-guard.sh :: AGENTS.md|CLAUDE.md) KIND="policy" ;;
.claude/hooks/context-budget-guard.sh :: CLAUDE.md is followed to its target, which is the cohort norm.
.claude/hooks/skills-submodule-update.sh :: # forever. The remedy is log-and-skip, not the exit-2 of AGENTS.md's gate
.claude/hooks/socraticode-health.sh :: # them was told by AGENTS.md to prefer codebase_search over grep.
.claude/hooks/socraticode-health.sh :: repo's AGENTS.md.
.pre-commit-config.yaml :: # worktree add` produces and which AGENTS.md's worktree-per-feature rule makes
.socraticodecontextartifacts.json :: "description": "Repository orientation \u2014 setup, the dev-server and port convention, the operational CLI list, and curated links into the canonical docs. The human entry point; AGENTS.md is the agent one."
.socraticodecontextartifacts.json :: "description": "The original MVP architecture design \u2014 the four-layer clearinghouse split (clearinghouse-core / domain-legislative / per-jurisdiction adapters / deployment API), provenance model, and adapter contract. Historical baseline: the layering has since grown to six layers (see AGENTS.md), but this remains the source of truth for the data model and the founding design decisions."
.socraticodecontextartifacts.json :: "path": "./AGENTS.md",
conftest_db.py :: #: Production secrets (AGENTS.md § Environment Variables). Deliberately *not* where
conftest_db.py :: nothing seeds it — while AGENTS.md § Server Lifecycle mandates worktree-based
deploy/usa-wa-committee-lineage-invariants.service :: # --frozen --no-sync: run against the installed venv as-is (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-house-corroboration.service :: # --frozen --no-sync: run against the installed venv as-is (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-integrity-sweep.service :: # at unit start (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-pdc-archive-refresh.service :: # at unit start (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-pdc-refresh.service :: # at unit start (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-senate-corroboration.service :: # --frozen --no-sync: run against the installed venv as-is (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-sos-archive-refresh.service :: # at unit start (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-sos-refresh.service :: # at unit start (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-succession-invariants.service :: # --frozen --no-sync: run against the installed venv as-is (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa-wsl-refresh.service :: # deliberate `uv sync` in the deploy runbook (AGENTS.md § Server Lifecycle, #30).
deploy/usa-wa.service :: # in the deploy runbook (AGENTS.md § Server Lifecycle, issue #30).
packages/usa-wa-adapter-legislature/tests/test_refresh_e2e.py :: #: spelled out — an assertion must never key on an exact upstream string (AGENTS.md).
packages/usa-wa-api/tests/test_log_config.py :: for doc in ("README.md", "AGENTS.md", "docs/COMMANDS.md"):
packages/usa-wa-common/src/usa_wa_common/parties.py :: **On holding a source's abbreviations in a source-free package (#227 CR #63).** AGENTS.md
packages/usa-wa-common/src/usa_wa_common/parties.py :: non-breaking space must not fall to ``unknown_token`` (CR #54, and AGENTS.md's rule against
packages/usa-wa-common/tests/test_parties.py :: AGENTS.md forbids. Case and edge whitespace fold; interior punctuation is significant."""
packages/usa-wa-common/tests/test_parties.py :: ``unknown_token``. AGENTS.md: never key a parser on an exact upstream string."""
packages/usa-wa-pipeline/src/usa_wa_pipeline/conformed/wire.py :: (the AGENTS.md rule: never key a parser on an exact upstream string; these
scripts/assert-venv-integrity.sh :: # It is coupled to the AGENTS.md § Project Layout `packages/*` workspace, on
scripts/assert-venv-integrity.sh :: # parser on an exact upstream string is what AGENTS.md § Project Layout
scripts/migrate.sh :: # deploy runbook (AGENTS.md § Server Lifecycle, issue #30).
scripts/pre-ship.sh :: # Dev/agent secrets, TEST_DATABASE_URL among them (AGENTS.md § Environment
scripts/pre-ship.sh :: # Loads usa-wa's two env files (AGENTS.md § Environment Variables), then hands
scripts/pre-ship.sh :: # The repo-wide idiom (AGENTS.md), deliberately reproduced rather than improved
scripts/pre-ship.sh :: # Worktrees don't populate submodules and AGENTS.md mandates worktree-based
scripts/pre-ship.sh :: # git-ignored, so the checkout AGENTS.md mandates feature work happen in (#87)
scripts/pre-ship.sh :: # nothing in worktree-create.sh seeds it — and AGENTS.md § Server Lifecycle
scripts/pre-ship.sh :: echo "            so it will not be committed (AGENTS.md § Environment Variables)." >&2
scripts/tests/test_assert_venv_integrity.py :: #: and AGENTS.md § Project Layout forbids keying a parser on an exact upstream
scripts/tests/test_context_manifest_drift.py :: artifacts=["./AGENTS.md", "./docs/API.md"],
scripts/tests/test_context_manifest_drift.py :: artifacts=["./AGENTS.md", "./docs/ARCHITECTURE.md"],
scripts/tests/test_context_manifest_drift.py :: artifacts=["./AGENTS.md", "./docs/specs"],
# Broad on purpose: five byte-identical fixture arguments in one test file.
# Content matching cannot split identical lines; one judgement covers all five.
scripts/tests/test_context_manifest_drift.py :: artifacts=["./AGENTS.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "CLAUDE.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "docs/API.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "docs/ARCHITECTURE.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "docs/MODULES-SPANS.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "docs/specs/2026-01-01-thing.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md", "packages/pkg/README.md"],
scripts/tests/test_context_manifest_drift.py :: docs=["AGENTS.md"],
scripts/tests/test_context_manifest_drift.py :: exempt="# a one-line include of AGENTS.md\nCLAUDE.md\n",
scripts/tests/test_context_manifest_drift.py :: repo = _scratch(tmp_path, artifacts=["./AGENTS.md"], docs=["AGENTS.md"])
scripts/tests/test_coverage_profiles.py :: DOCS_WITH_TEST_COMMANDS = ("AGENTS.md", "README.md", "docs/COMMANDS.md")
scripts/tests/test_doc_sections.py :: "AGENTS.md: project structure, conventions, skill inventory, route table",
scripts/tests/test_pre_ship_wrapper.py :: """Both files, in order — the repo-root one overrides, as AGENTS.md documents."""
scripts/tests/test_pre_ship_wrapper.py :: # worktree-create.sh seeds it. AGENTS.md § Server Lifecycle *mandates* worktree
scripts/tests/test_pre_ship_wrapper.py :: directory". Worktrees don't populate submodules, and AGENTS.md mandates
scripts/tests/test_test_db_remediation.py :: And the escape hatch it offered, ``-m 'not db'``, is the spelling AGENTS.md
scripts/tests/test_test_db_remediation.py :: Two things were wrong with that in the checkout AGENTS.md § Server Lifecycle
scripts/tests/test_test_db_remediation.py :: repo's deliberate secrets split (AGENTS.md § Environment Variables) is the one

# --- #406: docs/SOCRATICODE.md replaced docs/CODE-EXPLORATION.md -----------
# Class 1, back-references. The first four are written by the init-socraticode
# template itself (they are inside the socraticode-doc marker pair and a re-run
# rewrites them), and every § they name still exists in AGENTS.md:
# § Code Exploration Policy is the block this doc was split out of.
# measure-context reports dead_anchors: [].
docs/SOCRATICODE.md :: Detail doc for the `## Code Exploration Policy` block in `AGENTS.md`. That
docs/SOCRATICODE.md :: heading down there, not in `AGENTS.md`.
docs/SOCRATICODE.md :: `AGENTS.md` block should be on its degraded variant.
docs/SOCRATICODE.md :: it. That rule does not need to live in your `AGENTS.md` — the once-per-day hook
# Class 2, provenance-headings. All five were carried over unchanged from
# docs/CODE-EXPLORATION.md, where the same slugs were already load-bearing:
# docs/DEPLOYMENT.md links #the-server-is-pinned-not-installed-per-launch-389
# twice and docs/SKILLS.md links #manifest-coverage--the-drift-that-grows-silently-300.
docs/SOCRATICODE.md :: The file-dependency graph works here (#299)
docs/SOCRATICODE.md :: Dated prose is out of the code index (#406)
docs/SOCRATICODE.md :: Background work outlives the tool call (#299)
docs/SOCRATICODE.md :: Manifest coverage — the drift that grows silently (#300)
docs/SOCRATICODE.md :: The server is pinned, not installed per launch (#389)

# #237 branch, curating-context round (2026-09-24): the four docs split out of over-budget
# parents. Each back-reference below is the new doc's own "split out of" lead-in, or a
# pointer retargeted to where its content now lives — judged true against the split.
docs/COMMANDS-ROSTER.md :: Split out of [COMMANDS-BACKFILL.md](COMMANDS-BACKFILL.md); the index lives in
docs/COMMANDS-ROSTER.md :: [COMMANDS.md](COMMANDS.md). The Legislature's own roster PDF (#225, epic #219): the Phase A
docs/COMMANDS-ROSTER.md :: [MODULES-LEGISLATURE-ROSTER.md](MODULES-LEGISLATURE-ROSTER.md).
docs/DEPLOYMENT-HOST.md :: Split out of [`DEPLOYMENT.md`](DEPLOYMENT.md), which keeps the unit table, alerting, DB
docs/DEPLOYMENT-HOST.md :: The disk-GC commands came here from [`COMMANDS.md`](COMMANDS.md).
docs/MODULES-LEGISLATURE-ROSTER-BUILD.md :: [MODULES-LEGISLATURE-ROSTER.md](MODULES-LEGISLATURE-ROSTER.md), which keeps the source
docs/PIPELINE-CONFORMED-ENTITIES.md :: [`PIPELINE-CONFORMED.md`](PIPELINE-CONFORMED.md), which keeps the tenure spans and the
docs/PIPELINE-CONFORMED-ENTITIES.md :: engine as a Python model) landed next — see [`PIPELINE-CONFORMED.md`](PIPELINE-CONFORMED.md); roles/seats complete the
# A past-tense record of what the doc said when the #389 hole was found; retargeting it
# would falsify the history it reports.
deploy/system.slice.d/10-usa-wa-memory.conf :: # comment and docs/DEPLOYMENT.md both said it was protected. A protection that
